spring:
  datasource:
    url: jdbc:mysql://mysql:3306/loanmanager_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=America/Bogota&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME:loanmanager}
    password: ${DB_PASSWORD:LoanManager2025!}

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false

  mail:                          # ← dentro de spring:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:1234.carlosleal@gmail.com}
    password: ${MAIL_PASSWORD:vtpw ydwu spvh fhvl}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  devtools:
    restart:
      enabled: false

logging:
  level:
    com.loanmanager: INFO

jwt:
  secret: loanmanager-secret-key-very-long-and-secure-for-jwt-signing-2025
  expiration: 86400000
  prefix: Bearer
  header: Authorization

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui